package chapter15.functionalprogrammingrevisited;

public class ReturningOptional {
    /*
    * get()
    * ifPresent(Consumer)
    * isPresent()
    * orElse(T other)
    * orElseGet(Supplier s)
    * orElseThrow() NoSuchElementException
    * orElseThrow(Supplier s) () -> new IllegalStateException();
    *
    * */
}
